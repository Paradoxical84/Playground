apiVersion: v1
kind: Service
metadata:
  name: ai-lab
  namespace: ai-lab
  labels:
    app: ai-lab
spec:
  type: ClusterIP
  selector:
    app: ai-lab
  ports:
    - name: http
      port: 80
      targetPort: http
      protocol: TCP
