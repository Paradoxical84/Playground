apiVersion: v1
kind: Service
metadata:
  name: lab-api
  namespace: ai-lab
spec:
  type: NodePort
  selector:
    app: lab-api
  ports:
    - name: http
      port: 80
      targetPort: http
      nodePort: 30080
