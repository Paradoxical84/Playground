apiVersion: v1
kind: Service
metadata:
  name: ai-lab-nodeport
  namespace: ai-lab
  labels:
    app: ai-lab
spec:
  type: NodePort
  selector:
    app: ai-lab
  ports:
    - name: http
      port: 80
      targetPort: http
      nodePort: 30080
      protocol: TCP
